pluginManagement {
    repositories {
        mavenLocal()
        maven {
            name = 'comroid'
            url = 'https://maven.comroid.org'
        }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'ChatModLite'

include(':japi')
