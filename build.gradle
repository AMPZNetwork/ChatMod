project(':ChatMod:AurionChatApi') {
    it.ext {
        apiVersion = 'SNAPSHOT'
    }
    it.repositories.mavenCentral()
}

evaluationDependsOnChildren()

subprojects { p ->
    if (p.name != 'AurionChatApi')
        p.dependencies.compileOnly project(':ChatMod:AurionChatApi')
}
