project(':ChatMod:AurionChat:api') {
    it.ext {
        apiVersion = 'SNAPSHOT'
    }
    it.repositories.mavenCentral()
}

evaluationDependsOnChildren()

subprojects { p ->
    if (!p.path.endsWith('AurionChat:api'))
        p.dependencies.implementation project(':ChatMod:AurionChat:api')
}
